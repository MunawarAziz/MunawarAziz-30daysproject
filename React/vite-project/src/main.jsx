
import ReactDOM from 'react-dom/client';  // Ensure you're importing createRoot from the right place
import App from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <App />
);
